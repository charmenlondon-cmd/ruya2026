---
phase: 04-controller-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/controller/page.tsx
  - src/lib/hires.ts
  - src/components/controller/QuizScreen.tsx
  - src/components/controller/QuestionResultScreen.tsx
  - src/components/controller/FinalResultScreen.tsx
autonomous: true
---

<objective>
Build the quiz interface: question display with A/B/C answer buttons, per-question result feedback, and final score screen with hire logic.

Purpose: Delivers the core gameplay loop — the player answers 10 questions, sees correct/wrong feedback after each, and receives a hired/not-hired outcome.
Output: Working quiz flow covering question_active → answer_submitted → question_result → final_result, with hire records written to Supabase for passing scores (>=7).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-controller-ui/04-01-SUMMARY.md
@src/app/controller/page.tsx
@src/lib/session.ts
@src/lib/questions.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuizScreen — question display and answer submission</name>
  <files>
    src/components/controller/QuizScreen.tsx,
    src/app/controller/page.tsx
  </files>
  <action>
**Create src/components/controller/QuizScreen.tsx:**

Props: `session: Session`

1. Load questions on mount with a useEffect that fires when session.track and session.language are set:
   ```
   useEffect(() => {
     if (!session.track || !session.language) return
     getQuestionsForTrack(session.track, session.language).then(setQuestions).catch(setError)
   }, [session.track, session.language])
   ```
   Track `questions: Question[]` and `loadError: string | null` in local state.

2. Derive current question: `const question = questions[session.current_question]`

3. Show a question progress indicator at the top: "Question {session.current_question + 1} of 10" (text-white text-sm font-semibold).

4. Show question text in a white rounded card (bg-white/90 rounded-2xl p-6 text-aaah-dark-teal text-xl font-semibold text-center).

5. Show 3 answer buttons labeled A, B, C. For each answer slot (a, b, c):
   - If answer_x_image_url is non-null: render a Next.js <Image> (w-full max-h-36 object-contain rounded-xl)
   - Otherwise: render answer_x_text
   - Button wraps the content: bg-white/90 text-aaah-dark-teal rounded-2xl p-4 w-full text-left font-semibold hover:bg-white active:scale-95 transition-all
   - Each button starts with a coloured label badge: "A", "B", "C" (inline-flex justify-center items-center w-8 h-8 rounded-full bg-aaah-dark-teal text-white font-bold mr-3)

6. Track `answered: boolean` in local state (init false).

7. When session.state === 'question_active' and !answered:
   - On answer tap (A, B, or C):
     - Set answered = true (disables all buttons immediately)
     - Determine isCorrect: answer === question.correct_answer
     - Compute newScore: session.score + (isCorrect ? 1 : 0)
     - Call updateSession(session.id, { last_answer: answer as CorrectAnswer, last_answer_correct: isCorrect, score: newScore, state: 'answer_submitted' })

8. When session.state === 'answer_submitted': auto-advance to question_result after 1500ms.
   Use useEffect(() => { if (session.state !== 'answer_submitted') return; const t = setTimeout(() => updateSession(session.id, { state: 'question_result' }), 1500); return () => clearTimeout(t) }, [session.state])

9. Reset `answered` to false when session.current_question changes (useEffect dependency on session.current_question).

10. While questions are loading (questions.length === 0 and no error): show a spinner (animate-spin text-white).

**Update src/app/controller/page.tsx:**
Replace the "Quiz coming in Plan 04-02" placeholder with:
```
case 'question_active':
case 'answer_submitted':
  return <QuizScreen session={session} />
```
  </action>
  <verify>npx tsc --noEmit passes. Navigate /controller through full onboarding to question_active — verify question text and A/B/C buttons render. Tap an answer and verify session transitions to answer_submitted then question_result after 1500ms.</verify>
  <done>QuizScreen loads questions for the selected track/language. Tapping an answer updates session with last_answer, last_answer_correct, score, and state='answer_submitted'. After 1500ms, state auto-advances to question_result.</done>
</task>

<task type="auto">
  <name>Task 2: Result screens and hire logic</name>
  <files>
    src/lib/hires.ts,
    src/components/controller/QuestionResultScreen.tsx,
    src/components/controller/FinalResultScreen.tsx,
    src/app/controller/page.tsx
  </files>
  <action>
**Create src/lib/hires.ts:**
```typescript
import { supabase } from '@/lib/supabase'
import type { Session } from '@/types/database'

export async function createHire(session: Session): Promise<void> {
  const { error } = await supabase.from('hires').insert({
    session_id: session.id,
    player_name: session.player_name!,
    avatar_id: session.avatar_id!,
    track: session.track!,
    score: session.score,
  })
  if (error) throw new Error(`Failed to create hire: ${error.message}`)
}
```

**Create src/components/controller/QuestionResultScreen.tsx:**

Props: `session: Session`

1. Display correct/incorrect feedback based on session.last_answer_correct:
   - Correct: large green circle with ✓ (w-20 h-20 rounded-full bg-green-500 flex items-center justify-center text-white text-4xl), "Correct!" heading in green
   - Incorrect: large red circle with ✗ (bg-red-500), "Incorrect" heading in red
2. Show running score: "Score: {session.score} / {session.current_question + 1}" (text-white text-lg)
3. Button at bottom:
   - If session.current_question < 9: "Next Question →" → updateSession(session.id, { current_question: session.current_question + 1, state: 'question_active' })
   - If session.current_question === 9: "See Final Score →" → updateSession(session.id, { state: 'final_result' })
4. Button style: same large white teal-text style from onboarding screens (bg-white text-aaah-dark-teal font-semibold rounded-2xl px-10 py-5 text-lg)
5. Layout: centred column, content in a white/90 rounded card, button below the card

**Create src/components/controller/FinalResultScreen.tsx:**

Props: `session: Session`

1. Use a useRef `hireCreatedRef = useRef(false)` to guard against duplicate hire inserts.
2. On mount (useEffect(fn, [])): if session.score >= 7 and !hireCreatedRef.current, set hireCreatedRef.current = true and call createHire(session) (import from @/lib/hires). Log errors to console but don't block the UI.
3. Show the player's avatar: <Image src={`/avatars/${session.avatar_id}.png`} width={80} height={80} className="rounded-full" />
4. Show player name (text-white text-2xl font-bold)
5. Show final score prominently: "{session.score} / 10" (text-6xl font-bold)
   - Color: text-green-400 if score >= 7, text-white if score < 7
6. If score >= 7:
   - "You're Hired! 🎉" (text-3xl font-bold text-green-400)
   - Congratulations message (text-white text-lg): "Congratulations! You're the perfect fit for AAAH."
7. If score < 7:
   - "We'll Get Back to You" (text-3xl font-bold text-white)
   - Encouragement message: "Great effort! Keep developing your skills."
8. "Play Again" button at bottom: calls updateSession(session.id, { state: 'idle', language: null, player_name: null, avatar_id: null, track: null, current_question: 0, score: 0, last_answer: null, last_answer_correct: null })

**Update src/app/controller/page.tsx:**
Add to state router:
```
case 'question_result':
  return <QuestionResultScreen session={session} />
case 'final_result':
  return <FinalResultScreen session={session} />
```
  </action>
  <verify>npx tsc --noEmit passes. Complete a full game loop: onboarding → 10 questions answered → final result screen. Verify hires table in Supabase receives a record for score >= 7. Verify score < 7 shows "We'll Get Back to You" with no hire record.</verify>
  <done>Full game loop: 10 questions → per-question result screens → final score screen → "Play Again" resets to idle. Hire written to Supabase only for score >= 7. No duplicate hire inserts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit passes
- [ ] npm run build succeeds
- [ ] Full game loop works: onboarding → 10 questions → final result → play again
- [ ] hires table receives a record when score >= 7
- [ ] score < 7 shows "We'll Get Back to You" with no hire record created
- [ ] "Play Again" resets all session fields and returns to idle/language_select
</verification>

<success_criteria>
- All tasks completed
- No TypeScript errors
- Complete gameplay loop from question_active to final_result works
- Hire logic: score >= 7 writes to hires table exactly once; score < 7 does not
- "Play Again" resets all session fields and returns to idle state
</success_criteria>

<output>
After completion, create `.planning/phases/04-controller-ui/04-02-SUMMARY.md`
</output>
