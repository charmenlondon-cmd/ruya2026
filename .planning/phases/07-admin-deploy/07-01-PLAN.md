---
phase: 07-admin-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/page.tsx
autonomous: true
---

<objective>
Build the /admin page — a staff control panel for managing the game on event day.

Purpose: Staff need to start/reset sessions, clear players, clear hires, and force screensaver without touching Supabase directly.
Output: A functional, clearly laid out admin page with all required controls and a live session state viewer.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build /admin page</name>
  <files>src/app/admin/page.tsx</files>
  <action>
Replace the placeholder with a full admin panel. Controls required:
- New Session — createSession()
- Reset to Idle — updateSession patching all player fields to null + state: idle
- Clear Player — alias for Reset to Idle (same effect)
- Screensaver — updateSession({ state: screensaver })
- Clear All Hires — delete all rows from hires table
- Live session state viewer — formatted JSON of current session
  </action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Admin panel with all session controls and live state viewer</what-built>
  <how-to-verify>Visit http://localhost:3000/admin and test each button</how-to-verify>
  <resume-signal>Type "approved" to continue to 07-02</resume-signal>
</task>

</tasks>
