---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .eslintrc.json
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/controller/page.tsx
  - src/app/display/page.tsx
  - src/app/admin/page.tsx
autonomous: true
---

<objective>
Bootstrap the Next.js App Router project and scaffold the three core routes.

Purpose: Every subsequent plan builds on top of this — the project must compile and serve before Supabase or branding work can start.
Output: A working Next.js app (TypeScript + Tailwind + App Router) with placeholder pages at /, /controller, /display, and /admin.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js App Router project</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, .eslintrc.json, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Run `create-next-app` in the current directory. The directory already contains asset folders (Avatars/, AAAH Branding/, etc.) and a .git repo — these must not be touched.

Command:
```
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
```

If prompted about the non-empty directory, accept/continue. If prompted for package manager, use npm.

Expected project structure after init:
```
src/
  app/
    layout.tsx
    page.tsx
    globals.css
  lib/          (create manually if missing)
  types/        (create manually if missing)
  components/   (create manually if missing)
public/
next.config.ts
tailwind.config.ts
tsconfig.json
package.json
```

After init, create the empty directories that create-next-app won't add:
```
mkdir -p src/lib src/types src/components
```

Verify the project compiles:
```
npm run build
```
  </action>
  <verify>npm run build completes without errors</verify>
  <done>Build succeeds. `src/app/layout.tsx`, `src/app/page.tsx`, `tailwind.config.ts`, and `tsconfig.json` exist.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold /controller, /display, and /admin routes</name>
  <files>src/app/controller/page.tsx, src/app/display/page.tsx, src/app/admin/page.tsx</files>
  <action>
Create three route page files. Each is a minimal placeholder — just enough to confirm routing works. No logic, no styling beyond a label.

`src/app/controller/page.tsx`:
```tsx
export default function ControllerPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-2xl font-semibold">Controller</p>
    </main>
  )
}
```

`src/app/display/page.tsx`:
```tsx
export default function DisplayPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-2xl font-semibold">Display</p>
    </main>
  )
}
```

`src/app/admin/page.tsx`:
```tsx
export default function AdminPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-2xl font-semibold">Admin</p>
    </main>
  )
}
```

After creating, run `npm run build` again to confirm all three routes compile.
  </action>
  <verify>npm run build succeeds with routes for /, /controller, /display, /admin shown in output</verify>
  <done>All four routes compile. Navigating to /controller, /display, /admin in dev returns a page (not 404).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes with zero errors
- [ ] `src/app/controller/page.tsx`, `src/app/display/page.tsx`, `src/app/admin/page.tsx` exist
- [ ] `src/lib/`, `src/types/`, `src/components/` directories exist
- [ ] No asset files from `Avatars/`, `AAAH Branding/` etc. were moved or deleted
</verification>

<success_criteria>

- Next.js App Router project initialised with TypeScript, Tailwind, ESLint
- All four routes scaffold and compile
- Project structure ready for parallel Supabase + branding work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
