---
phase: 05-display-screen
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/display/ScreensaverScreen.tsx
  - src/app/display/page.tsx
autonomous: false
---

<objective>
Build the ScreensaverScreen for idle/screensaver states, wire it into the display page, and verify the complete display experience end-to-end.

Purpose: The screensaver is the baseline state — it plays when no game is active and between players. It should make a strong AAAH brand impression and invite passers-by to pick up the iPad. Phase 6 will add the hired network of avatars on top of this screen; design it as a clean, extensible base layer.

Output: Animated ScreensaverScreen, display page routing complete, human verification of the full display experience.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/database.ts
@src/app/display/page.tsx
@src/app/globals.css
@.planning/phases/05-display-screen/05-01-SUMMARY.md
@.planning/phases/05-display-screen/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScreensaverScreen + wire into display page</name>
  <files>src/components/display/ScreensaverScreen.tsx, src/app/display/page.tsx</files>
  <action>
**src/components/display/ScreensaverScreen.tsx:**
- No props needed (no active session data to display)
- `'use client'` directive
- Layout: `flex-1 flex flex-col items-center justify-center gap-8 relative overflow-hidden`
- Content (centred, large, TV-scale):
  1. AAAH logo: `<Image src="/logos/aaah-logo-white.png" width={280} height={75} className="h-20 w-auto object-contain opacity-90" alt="Abdulla Al Arif Holding" />`
  2. Heading: `"Ruya Careers Fair 2026"` in `text-6xl font-bold text-white text-center`
  3. Tagline: `"Pick up the iPad to start playing!"` in `text-3xl text-aaah-light-teal text-center animate-pulse`

- **Animation — keep it CSS-only (no Framer Motion yet; Phase 6 adds D3/Canvas):**
  - The entire content group gets a gentle float: wrap in a `<div className="flex flex-col items-center gap-8 animate-[float_6s_ease-in-out_infinite]">`
  - Add the `float` keyframe animation via an inline `<style>` tag in the component (or via a wrapper class):
    ```css
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-16px); }
    }
    ```
    Use a `<style>{...}</style>` JSX element inside the component to define this keyframe, since Tailwind v4 arbitrary keyframes are verbose. Keep it minimal.
  - Background: the aaah-gradient is already applied by the root layout body — no additional background needed. Optionally add `radial-gradient` behind the logo for a subtle glow effect using an absolutely-positioned div.

- **Phase 6 note (comment in component):** Phase 6 will render the hired-network visualization (D3/Canvas) as an absolutely-positioned layer behind this content. Keep the content layer at `z-10` and the background at `z-0` so the network layer can slot in at `z-5`.

**src/app/display/page.tsx:**
1. Add import: `import { ScreensaverScreen } from '@/components/display/ScreensaverScreen'`
2. In the switch statement, replace the `'idle' | 'screensaver' | default` inline placeholder with:
   ```
   case 'idle':
   case 'screensaver':
   default:
     return <ScreensaverScreen />
   ```
No other changes to page.tsx.
  </action>
  <verify>npx tsc --noEmit passes; npm run build succeeds</verify>
  <done>ScreensaverScreen renders with float animation; display/page.tsx routes idle and screensaver to it; TypeScript clean; build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete display screen experience:
- WaitingScreen (player onboarding states)
- QuestionScreen (live question display with player info bar, A/B/C answer grid)
- FinalResultScreen (hired / not-hired result)
- ScreensaverScreen (animated AAAH screensaver)
  </what-built>
  <how-to-verify>
1. Run: `npm run dev`
2. Open two browser tabs/windows:
   - Controller: http://localhost:3000/controller
   - Display: http://localhost:3000/display (simulate the TV — make this window wide/large)

3. **Screensaver check** (display should show screensaver by default — idle state):
   - Display shows AAAH logo + "Ruya Careers Fair 2026" + "Pick up the iPad to start playing!" with gentle float animation

4. **WaitingScreen check** — on the controller, tap "Start" then choose a language:
   - Display immediately shows "A player is joining…" with player status updating live

5. **QuestionScreen check** — complete onboarding on the controller (avatar → name → track), game starts:
   - Display shows full question screen: player info bar at top (avatar, name, track, score), question card, 3-column A/B/C answer options
   - Tap answers on controller — confirm question advances on display in real time
   - Confirm score updates in the player bar after correct answers

6. **FinalResultScreen check** — complete all 10 questions:
   - If score ≥ 7: display shows "You're Hired! 🎉" with green accents, large avatar + name + score
   - If score < 7: display shows "We'll Get Back to You" with teal accents
   - Tap "Play Again" on controller — display returns to screensaver

7. **RTL check** — run a second game in Arabic:
   - Choose "العربية" on controller; verify display shows Arabic text with correct RTL layout

8. Check: text is readable (not tiny), layout fills the browser window appropriately
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete (after checkpoint approval):
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `npm run build` — clean build
- [ ] All 4 display screens verified by human
- [ ] Real-time Realtime sync confirmed (display updates when controller advances)
</verification>

<success_criteria>

- All tasks completed and committed
- ScreensaverScreen has float animation and shows AAAH branding
- display/page.tsx routes all 10 session states correctly — no state falls through unhandled
- Human verified all 4 screens look correct and Realtime sync works
- Phase 05 complete — ready for Phase 06 (Hired Network)
  </success_criteria>

<output>
After completion, create `.planning/phases/05-display-screen/05-03-SUMMARY.md`
</output>
