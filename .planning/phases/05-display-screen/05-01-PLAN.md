---
phase: 05-display-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/components/display/WaitingScreen.tsx
  - src/components/display/QuestionScreen.tsx
  - src/app/display/page.tsx
autonomous: true
---

<objective>
Replace the display scaffold with the full real-time display experience for active gameplay — the view the audience sees throughout the quiz.

Purpose: The display screen is projected on a large TV at the careers fair. This plan builds what 99% of the audience sees: a player joining → question displayed live → repeat for 10 questions.

Output: Working display page that routes all session states, showing WaitingScreen during onboarding and a full-screen QuestionScreen during the quiz. Inline placeholders for result/screensaver states (replaced in 05-02/05-03 without breaking the build).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/database.ts
@src/hooks/useSession.ts
@src/lib/i18n.ts
@src/lib/questions.ts
@src/app/display/page.tsx
@src/app/controller/page.tsx
@src/components/controller/QuizScreen.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n addition + WaitingScreen</name>
  <files>src/lib/i18n.ts, src/components/display/WaitingScreen.tsx</files>
  <action>
**src/lib/i18n.ts** — add one new key to both `en` and `ar` string maps (additive only, no changes to existing keys):
- `playerJoining: 'A player is joining…'` (en)
- `playerJoining: '…لاعب ينضم'` (ar)

**src/components/display/WaitingScreen.tsx** — create this component:
- Props: `{ session: Session, language: Language }`
- `'use client'` directive
- Purpose: shown when `session.state` is any onboarding step (`language_select`, `avatar_select`, `name_entry`, `track_select`) — the player is picking their preferences on the controller
- Layout: full-screen vertically and horizontally centred, designed for a large landscape TV
- Structure (top to bottom):
  1. `t(language).playerJoining` text in small caps or light weight — status line
  2. If `session.avatar_id` is set: `<Image src={"/avatars/" + session.avatar_id + ".png"} width={160} height={160} className="rounded-full" alt="..." />` centred (use Next.js Image)
  3. If `session.player_name` is set: player name in very large bold white text (`text-6xl font-bold text-white`)
  4. If `session.track` is set: track name via `t(language).trackName(session.track)` in large teal text (`text-3xl text-aaah-light-teal`)
  5. Animated loading dots or a subtle pulse on the status line while waiting (Tailwind `animate-pulse` on the status text)
- When none of avatar/name/track are set yet (pure language_select state): just show "AAAH Careers Fair 2026" heading + the playerJoining status line
- All text: white or aaah-light-teal, readable at 5 metres
  </action>
  <verify>npx tsc --noEmit (the existing display/page.tsx scaffold still compiles; i18n.ts compiles clean)</verify>
  <done>i18n has playerJoining in both locales; WaitingScreen renders progressive player info without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: QuestionScreen + display page orchestrator</name>
  <files>src/components/display/QuestionScreen.tsx, src/app/display/page.tsx</files>
  <action>
**src/components/display/QuestionScreen.tsx** — the main audience-facing game view:
- Props: `{ session: Session, language: Language }`
- `'use client'` directive
- Loads questions from Supabase: `getQuestionsForTrack(session.track!, session.language!)` in useEffect (same approach as controller's QuizScreen but no optimistic index — use `session.current_question` directly since display is purely reactive)
- While loading: centered white spinner (`animate-spin border-4 border-white border-t-transparent rounded-full w-16 h-16`)
- On load error: `<p className="text-red-300 text-2xl text-center">`
- When question loaded: full-screen TV layout (two-section vertical stack):

  **Top bar** (`flex items-center justify-between px-8 py-4 bg-black/20 backdrop-blur-sm`):
  - Left: avatar (32×32 rounded-full) + player name (`text-2xl font-bold text-white`) + track (`text-lg text-aaah-light-teal`) — all in a flex row with gap
  - Right: question counter `t(language).questionOf(session.current_question + 1, 10)` + score `t(language).score(session.score, session.current_question)` stacked vertically, right-aligned, white text

  **Main content area** (`flex-1 flex flex-col items-center justify-center gap-8 px-12 py-8`):
  - Question card: `bg-white/90 rounded-3xl p-10 w-full max-w-5xl` containing question text in `text-aaah-dark-teal text-3xl font-semibold text-center leading-relaxed`
  - Answer options row: `grid grid-cols-3 gap-6 w-full max-w-5xl` — 3 equal columns
    - Each option: `bg-white/80 rounded-2xl p-6 flex flex-col items-center gap-4`
    - Answer key badge: `w-12 h-12 rounded-full bg-aaah-dark-teal text-white text-2xl font-bold flex items-center justify-center flex-shrink-0`
    - Text: `text-aaah-dark-teal text-xl font-semibold text-center`
    - Image answer (when `answer_X_image_url` is non-null): `<Image width={280} height={200} className="object-contain rounded-xl w-full max-h-48" />`
  - For RTL (Arabic), the grid columns stay left-to-right for A/B/C visual order (answers are visual, not textual flow)
- NO click handlers — display screen is purely passive, read-only

**src/app/display/page.tsx** — replace the scaffold entirely:
- `'use client'`
- Import: `useSession`, `t`, `WaitingScreen`, `QuestionScreen`, `Language`, `Session`
- Call `useSession()` → `{ session, loading, error }`
- `const language: Language = session?.language ?? 'en'`
- `const dir = language === 'ar' ? 'rtl' : 'ltr'`
- Outermost: `<div dir={dir} className="min-h-[calc(100vh-56px)] flex flex-col">`
  - Loading: centred white spinner
  - Error: `<p className="text-white text-xl text-center p-8">Error: {error}</p>`
  - No session: `<p className="text-white text-xl text-center p-8">Waiting for session…</p>`
  - Session present: switch on `session.state`:
    - `'language_select' | 'avatar_select' | 'name_entry' | 'track_select'`: `<WaitingScreen session={session} language={language} />`
    - `'question_active' | 'answer_submitted' | 'question_result'`: `<QuestionScreen session={session} language={language} />`
    - `'final_result'`: inline placeholder — `<div className="flex-1 flex flex-col items-center justify-center"><p className="text-white text-5xl font-bold">The results are in…</p></div>`
    - `'idle' | 'screensaver' | default`: inline placeholder — `<div className="flex-1 flex flex-col items-center justify-center gap-6"><h1 className="text-white text-6xl font-bold">Ruya Careers Fair 2026</h1><p className="text-aaah-light-teal text-2xl">Pick up the iPad to start playing!</p></div>`
  </action>
  <verify>npx tsc --noEmit passes; npm run build succeeds</verify>
  <done>Display page compiles and routes; QuestionScreen shows question + A/B/C layout; WaitingScreen shows progressive player info; TypeScript clean; build passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `npm run build` — clean build, no warnings
- [ ] No unused imports in any new file
</verification>

<success_criteria>

- All tasks completed and committed
- `src/lib/i18n.ts` has `playerJoining` in both locales
- `WaitingScreen` progressively reveals avatar → name → track as session fills in
- `QuestionScreen` loads and displays question + 3 answers in a TV-optimised landscape layout (top player bar, question card, 3-col answer grid)
- `display/page.tsx` routes all 10 session states without TypeScript errors (inline placeholders for final_result and idle/screensaver)
- Build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/05-display-screen/05-01-SUMMARY.md`
</output>
