---
phase: 05-display-screen
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/display/FinalResultScreen.tsx
  - src/app/display/page.tsx
autonomous: true
---

<objective>
Build the display-screen FinalResultScreen — the big moment the audience sees when a player finishes all 10 questions.

Purpose: The hired / not-hired reveal is the emotional peak of the game. On the display TV it needs to be visually striking, legible from across the room, and clearly differentiated between the two outcomes.

Output: FinalResultScreen component wired into display/page.tsx, replacing the "The results are in…" placeholder.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/database.ts
@src/lib/i18n.ts
@src/app/display/page.tsx
@src/components/controller/FinalResultScreen.tsx
@.planning/phases/05-display-screen/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Display FinalResultScreen component</name>
  <files>src/components/display/FinalResultScreen.tsx</files>
  <action>
Create `src/components/display/FinalResultScreen.tsx`:
- Props: `{ session: Session, language: Language }`
- `'use client'` directive
- `const isHired = session.score >= 7`
- `const strings = t(language)`

This is the TV-facing result screen — no buttons, no interactions. Pure display. Designed to be read from 5+ metres.

**Hired outcome (`isHired === true`):**
Full-screen layout with green celebration palette on top of the gradient background:
- Top half: large avatar (`<Image src={"/avatars/" + session.avatar_id + ".png"} width={200} height={200} className="rounded-full border-8 border-green-400 shadow-2xl" />`)
- Player name: `text-7xl font-bold text-white` — massive, unmissable
- Track: `text-3xl text-aaah-light-teal`
- Score: `text-8xl font-bold text-green-400` e.g. "9 / 10"
- Outcome heading: `strings.youreHired` in `text-5xl font-bold text-green-300`
- Sub-message: `strings.hiredMessage` in `text-2xl text-white/80 text-center max-w-2xl`
- Optional: add a subtle pulsing ring behind the avatar (`animate-ping` on an absolutely positioned div, very subtle, `opacity-20`)

**Not-hired outcome (`isHired === false`):**
Same layout structure, different palette — warm and dignified, not deflating:
- Avatar: same size, border `border-aaah-light-teal`
- Player name: `text-7xl font-bold text-white`
- Track: `text-3xl text-aaah-light-teal`
- Score: `text-8xl font-bold text-aaah-light-teal`
- Outcome heading: `strings.wellGetBack` in `text-5xl font-bold text-white`
- Sub-message: `strings.getBackMessage` in `text-2xl text-white/80 text-center max-w-2xl`

**Container** (both outcomes): `flex-1 flex flex-col items-center justify-center gap-6 px-12 py-8`

Use `strings.finalScore(session.score)` for the score display (returns `"X / 10"` already).
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>FinalResultScreen renders correctly for both isHired=true and isHired=false without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire FinalResultScreen into display page</name>
  <files>src/app/display/page.tsx</files>
  <action>
Update `src/app/display/page.tsx`:
1. Add import: `import { FinalResultScreen } from '@/components/display/FinalResultScreen'`
2. In the switch statement, replace the `'final_result'` inline placeholder div with:
   `case 'final_result': return <FinalResultScreen session={session} language={language} />`

No other changes to page.tsx. The file was created in 05-01 — only these two targeted edits.
  </action>
  <verify>npx tsc --noEmit passes; npm run build succeeds</verify>
  <done>display/page.tsx imports FinalResultScreen; final_result state renders the new component; build clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `npm run build` — clean build
- [ ] FinalResultScreen has distinct visuals for hired vs not-hired
- [ ] All text readable at TV scale (minimum text-3xl for supporting copy, text-6xl+ for hero text)
</verification>

<success_criteria>

- All tasks completed and committed
- FinalResultScreen component exists with TV-scale typography
- Hired outcome: green accents, large avatar, celebration feel
- Not-hired outcome: teal accents, dignified, positive tone
- display/page.tsx routes `final_result` to FinalResultScreen
- TypeScript clean, build passes
  </success_criteria>

<output>
After completion, create `.planning/phases/05-display-screen/05-02-SUMMARY.md`
</output>
